# 常见面试问答（FAQ）

> 本文件是检索命中的核心：面试官高频问题在此预写答案，保证稳定高质量回答。
> 【待填】处请用户补充真实信息。技术八股类默认不收录答案（知识库外如实说明），是否补充由用户决定。

## 自我介绍类

### 请做一下自我介绍
我是郑梓恒，Java/Go 后端方向的求职者。独立搭建过两个代表性项目：RAG 知识库问答系统（Vue3 + Spring Boot + PGVector，混合检索 + Agent 化问答 + 评测闭环），以及健身小程序的统一后端（图片托管 + AI Agent）。技术栈覆盖 Java/Spring、Go、MySQL/PostgreSQL、Vue3、Docker、CI。【待填：补充教育背景/实习经历】

### 你为什么选择后端方向
【待填】个人原因，如：喜欢把业务逻辑抽象成稳定的服务、对数据/并发/系统设计感兴趣、享受从 0 搭建完整系统的过程。

### 介绍一下你最有代表性的项目
最有代表性的是 RAG 知识库问答系统。它实现了一条完整的 RAG 管线：Markdown 按标题分块、混合检索（向量+关键词 RRF 融合）、Rerank、LLM 流式生成，还做了 Agent 化多步问答（ReAct：检索→反思→再检索）和评测闭环（Recall@K + LLM-as-judge）。配套了 45+10 后端测试、23 前端用例和 CI，形成"开发-评测-反馈-调优"闭环。

### 你的技术栈主要有哪些
后端 Java/Spring Boot、Go，数据库 MySQL/PostgreSQL（含 pgvector），前端 Vue3，以及 RAG/Agent 相关的 Embedding、混合检索、LLM 接入。工程化上有 Docker、GitHub Actions CI、JUnit/Vitest 测试。

## 项目深挖类

### RAG 项目里最难的点是什么
中文检索召回质量。单纯向量检索对中文术语、缩写召回差。我用混合检索解决：向量召回（语义）+ 关键词召回（中文 bigram 分词 + pg_trgm 字面匹配），再用 RRF 倒数排名融合，最后 Rerank 精排，评测集验证 Recall@K 有明显提升。

### 为什么用 RRF 融合而不是加权求和
因为两路召回的分数尺度不同：向量路是余弦距离，关键词路是命中次数，直接加权要调权重且不稳。RRF 只关心排名（1/(k+rank) 求和），免调参、鲁棒，k 取常用值 60。

### 分块策略是怎么定的
Markdown 按标题（##/###）切成结构块，保证块有语义边界；长块再按长度二次切分，用滑动窗口 + 句末标点优先，相邻块保留重叠避免边界信息丢失。块大小与重叠都做成配置（max-chars / overlap）。

### Agent 模式是怎么实现的
ReAct 风格循环：LLM 输出 THOUGHT/ACTION/ACTION_INPUT/ANSWER，解析后分派工具（语义检索/关键词检索/列文档/读文档），结果按全局编号回填，最多 5 步；最终答案的 [N] 与引用来源严格对应。还有"过早放弃"拦截——模型说"没找到"但实际没检索时，强制补一次语义检索再答。

### 引用溯源怎么保证准确
所有工具命中的片段统一收集去重，按全局编号回填上下文，最终答案里 [N] 与返回的 sources[N-1] 一一对应，前端可点击查看原文章节，避免模型编造引用。

### 评测闭环是怎么做的
建评测集（问题 + 期望关键词 + 参考答案），跑 Recall@K（命中期望关键词比例）+ LLM-as-judge 打分，生成 HTML 报告可回溯每题；差评经点赞/点踩反馈回流，人工复核后扩充评测集，形成数据驱动优化。

### 健身小程序后端做了什么
它是统一独立后端，承载两类能力：图片托管（1324 个动作 GIF 的 exerciseId→URL 映射）和 AI Agent（chat/search/insight）。特点是 embedding 用 MySQL JSON 存向量、应用内算余弦，不引入 PGVector——因为只有 1324 条数据，全量载入内存算余弦毫秒级即可。

### 为什么健身项目不用 PGVector 而 rag 项目用了
数据规模决定。健身项目只有 1324 条动作数据，MySQL JSON 存向量 + 应用内余弦就够了，省一套 PostgreSQL 运维；rag 项目要做通用知识库问答，用 PGVector + HNSW 索引更合适。

## 技术八股类（是否收录由用户决定）

> 默认不收录答案。若希望 AI 分身能答八股题，请在此逐条补充答案；否则面试官问八股时 AI 会如实说明"简历/经历中没有这方面的内容"。

- Java 并发：volatile、synchronized、线程池参数
- Spring：IoC/AOP、事务传播、循环依赖
- MySQL：索引（B+ 树）、事务隔离级别、MVCC
- Redis：缓存穿透/击穿/雪崩、持久化
- 网络：TCP 三次握手、HTTP/HTTPS
- 系统设计：限流、幂等、分布式锁

## 行为类

### 你最大的技术挫折是什么，怎么解决的
【待填】请补充真实经历，如：某个 bug/性能问题排查了多久、最终如何定位与解决、学到了什么。

### 你是怎么学习新技术的
【待填】如：带着项目学（rag-kb-demo 就是边做边学 RAG）、看官方文档 + 源码 + 写 demo 验证。

### 你的职业规划是什么
【待填】如：短期深耕后端与 AI 应用工程化，长期往系统设计/技术专家方向发展。

### 为什么独立做这两个项目
【待填】如：通过完整项目把 RAG/Agent 从原理到工程吃透，同时作为求职的"可验证作品"。

### 你的优势和待提升的地方
优势：【待填】（如：独立完成全栈项目、工程化意识强、能写测试与 CI）。待提升：【待填】。
