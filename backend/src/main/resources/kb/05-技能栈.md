# 技能栈

> 以下为草稿（基于已知项目推断），请按真实掌握程度增删改。

## 后端
- Java（集合/IO/多线程/并发）、Spring Boot、MyBatis(-Plus)、Spring Security(JWT)
- Go（有使用经历，写过 m3u8 下载器等工具）
- 关系型数据库：MySQL（索引/优化）、PostgreSQL（pgvector 向量扩展）

## RAG / AI 应用（代表性方向）
- RAG 管线：Markdown 分块、Embedding、混合检索（向量+关键词 RRF 融合）、Rerank、引用溯源
- Agent 化问答：ReAct 多步（检索→反思→再检索）、工具调用、SSE 流式 + 思考动效
- 评测闭环：评测集 + Recall@K + LLM-as-judge
- 大模型接入：OpenAI 兼容协议（DeepSeek）、Embedding（bge-m3）

## 前端
- Vue 3（Composition API）、Vite、TypeScript、Pinia、Vue Router、Element Plus
- SSE 流式渲染、状态管理、响应式布局

## 工程化
- Docker（多阶段构建）、Docker Compose、Nginx、GitHub Actions CI
- 测试：JUnit、Testcontainers、Vitest、JaCoCo 覆盖率门禁
- 数据库迁移 Flyway、Git 分支流

## 缓存 / 中间件（了解）
- Redis（缓存、限流计数）、消息队列（了解）

## 工具
- Git、Linux、IDEA、Postman、Swagger/OpenAPI
