# 项目状态快照（上下文压缩存档）

> 更新时间：2026-08-19。本文件是"开发到现在为止"的精简状态,新会话/上下文压缩后以此为准。

## 项目信息
- 路径：`E:\AgentData\DeepSeek\personal-agent-site`（git 已提交,分支 master）
- 文档：`docs/` 下有 PLAN / 需求分析 / 可行性分析 / 技术方案 / 开发问题记录 / 本快照
- 目标：个人 AI 分身站——面试官可先看简历页、再与 AI 分身对话（第一人称、只答知识库、引用可溯源）

## 已完成 ✅
### 后端（Spring Boot,包名 me.zhengziheng.agent）
- 人设 PersonaPrompts（Agent 开发方向,2026 届,技术八股用自身知识答,隐私拒答）
- KbSeedService：启动幂等灌库 + 孤儿清理（删文件即清库,已提前到灌入前）
- 限流 50 问/时 + 登录 10 次/时；敏感词护栏（仅隐私意图）
- 站长账号配置注入 + /api/admin/* 鉴权；管理接口（qa 列表/反馈统计/kb 重建）
- Agent：强制先检索、get_document 已移除、最终答案 SSE 流式、thinking 思考事件
- 评测：EvalService 用人设提示词 + 宽松裁判 + isRefusal 兼容人设措辞；28 题评测集
- 环境注意：本机 Java 19 残缺,用 **JBR 17**（`D:\IntelliJ IDEA 2022.3.1\jbr`）,java.version=17

### 前端（Vue3,工程档案风 UI——用户重设计）
- 落地页 / 聊天页（流式 + 思考块 + [N] 引用角标 + **Markdown 渲染** marked+DOMPurify）/ 管理页（数据总览 + 评测标签页）
- vite 代理：只对 /chat/stream 设 Accept: text/event-stream（勿全局设,会 406）
- SSE 断开自动轮询兜底

### 知识库（backend/src/main/resources/kb/,7 篇）
- 01 简介 / 02 教育 / 03 经历与项目实践（无实习口径）/ 04-rag-kb-demo / 04-fitlog（小程序 CloudBase 故事,已合并重复篇）/ 05 技能栈 / 06 FAQ

### M2 评测
- 28 题（fact 21/cross 4/reject 3）,召回 93-95%、忠实度 84-96%、隐私拒答 OK
- 管理页"评测"标签页可跑可看；接口已包 Result 信封

## 未完成 ⏳
- **M3 上线**（当前进行中）：服务器已买（见下）→ CI workflow 未写 → 部署未做 → 域名未注册/解析
- 内容收尾：01 工作地点/入职时间、无实习真实原因、FAQ 行为类"优势/待提升"仍【待填】
- M2 迭代：FitLog 合并后重跑评测、差评人工复核、normal 对照(mode=vector)、FAQ 补 30-50 题
- 旧 DeepSeek key 作废（rag-kb-demo .env.example 泄露的那个）

## 服务器信息
- IP：103.212.187.133（香港,免备案；root 密码见对话,建议上线后改）
- 计划：Ubuntu 22.04 + Docker（只装这个,零构建）+ swap 1-2G；安全组放行 22/80/443
- 部署：deploy/docker-compose.yml（pg+backend+nginx 镜像从 GHCR 拉）+ nginx.conf + .env.example

## 下一步（M3 部署线）
1. SSH 连服务器 → 装 Docker + swap
2. 写 CI workflow（GitHub Actions 构建 backend/frontend 镜像推 GHCR）
3. 域名 zhengziheng.me 注册 + 实名 + A 记录解析到 103.212.187.133
4. 服务器 git clone + .env 填 key → docker compose pull && up -d
5. certbot HTTPS + 上线检查清单

## 关键排障记忆（详见 docs/开发问题记录.md）
- PS 5.1 改文本必须显式 UTF-8；vite Accept 头勿全局设；SSE 超时已 600s；
- 沙箱网络受限（硅基流动/ghcr 等可能 DNS 失败）,服务器操作走 SSH 更稳。
